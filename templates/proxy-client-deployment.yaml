{{- if .Values.proxy-client.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxy-client
spec:
  replicas: {{ .Values.proxy-client.replicas }}
  selector:
    matchLabels:
      app: proxy-client
  template:
    metadata:
      labels:
        app: proxy-client
    spec:
      containers:
      - name: proxy-client
        image: {{ .Values.proxy-client.image.repository }}:{{ .Values.proxy-client.image.tag }}
        ports:
        - containerPort: {{ .Values.proxy-client.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.proxy-client.env.SPRING_PROFILES_ACTIVE }}
        - name: SPRING_ZIPKIN_BASE_URL
          value: {{ .Values.proxy-client.env.SPRING_ZIPKIN_BASE_URL }}
        - name: EUREKA_CLIENT_REGION
          value: {{ .Values.proxy-client.env.EUREKA_CLIENT_REGION }}
        - name: EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT
          value: {{ .Values.proxy-client.env.EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT }}
        - name: EUREKA_CLIENT_SERVICEURL_MYZONE
          value: {{ .Values.proxy-client.env.EUREKA_CLIENT_SERVICEURL_MYZONE }}
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: {{ .Values.proxy-client.env.EUREKA_CLIENT_SERVICEURL_DEFAULTZONE }}
        - name: SPRING_CONFIG_IMPORT
          value: {{ .Values.proxy-client.env.SPRING_CONFIG_IMPORT }}
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: {{ .Values.proxy-client.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
      initContainers:
      - name: wait-for-dependencies
        image: busybox
        command: ['sh', '-c', 'until nc -z zipkin 9411 && nc -z cloud-config 9296 && nc -z service-discovery 8761; do echo waiting for dependencies; sleep 2; done;']
{{- end }}