{{- if .Values.user-service.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: {{ .Values.user-service.replicas }}
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: {{ .Values.user-service.image.repository }}:{{ .Values.user-service.image.tag }}
        ports:
        - containerPort: {{ .Values.user-service.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.user-service.env.SPRING_PROFILES_ACTIVE }}
        - name: SPRING_ZIPKIN_BASE_URL
          value: {{ .Values.user-service.env.SPRING_ZIPKIN_BASE_URL }}
        - name: EUREKA_CLIENT_REGION
          value: {{ .Values.user-service.env.EUREKA_CLIENT_REGION }}
        - name: EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT
          value: {{ .Values.user-service.env.EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT }}
        - name: EUREKA_CLIENT_SERVICEURL_MYZONE
          value: {{ .Values.user-service.env.EUREKA_CLIENT_SERVICEURL_MYZONE }}
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: {{ .Values.user-service.env.EUREKA_CLIENT_SERVICEURL_DEFAULTZONE }}
        - name: SPRING_CONFIG_IMPORT
          value: {{ .Values.user-service.env.SPRING_CONFIG_IMPORT }}
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: {{ .Values.user-service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
      initContainers:
      - name: wait-for-dependencies
        image: busybox
        command: ['sh', '-c', 'until nc -z zipkin 9411 && nc -z cloud-config 9296 && nc -z service-discovery 8761; do echo waiting for dependencies; sleep 2; done;']
{{- end }}